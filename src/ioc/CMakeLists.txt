set(IOC_INCLUDE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/include")

if (BUILD_TESTING)
  add_subdirectory("${CMAKE_CURRENT_SOURCE_DIR}/tests")
endif()

install(DIRECTORY ${IOC_INCLUDE_DIR} DESTINATION ${CMAKE_INSTALL_PREFIX})
